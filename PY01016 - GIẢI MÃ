t = int(input())
for _ in range(t) :
    a = input()
    res =""
    while(len(a)!=0) :
        if a[0].isalpha() : 
            c = a[0]
            a =a[1:]
        elif a[0].isdigit() :
            res += c * int(a[0])
            a = a[1:]
    print(res)
